#### 依赖修改
通过将scope设置为provided将jdbc测试驱动依赖p6spy去除（正式环境不使用该依赖，该依赖用于测试环境输出测试SQL）
		
		<dependency>
			<groupId>p6spy</groupId>
			<artifactId>p6spy</artifactId>
			<version>3.8.2</version>
			<scope>provided</scope>
		</dependency>
		
#### 修改配置
根据服务配置说明对服务配置进行修改，注意将激活profile变更为pro标签,并清理刷新maven依赖

#### 打包编译
待描述